pipeline{
  agent { node { lable "node1"} }
  
  stages {
    stage("build"){
      steps{
        script{
          mvn_home = tool "MAVEN_HOME"
          sh "${mvn_home/bin/mvn -v}"
        }
      }
    }
  }
}
